# What goes in

The input boundary accepts five classes from the walkthrough: **prompt**, **files/docs metadata or extracted content**, **images metadata or extracted observations**, **tool results**, and **history**. Inputs are optional and are combined only when explicitly supplied to the runtime.
