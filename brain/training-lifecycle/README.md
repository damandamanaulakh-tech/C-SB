# Training lifecycle

The walkthrough's lifecycle is represented as: data collection -> pretraining -> fine-tuning -> alignment -> evaluation -> deployment. Runtime feedback does not itself retrain the model. Any future training dataset must be deliberately curated, reviewed, evaluated, and promoted through a separate lifecycle.
