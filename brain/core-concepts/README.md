# Core concepts

The architecture tracks parameters, tokens, context window, embeddings, attention, layers, logits, decoding, RAG, and tools. Model-internal concepts remain explicitly opaque unless supported telemetry is provided by an actual model API. Sourceborn focuses on provenance, context, routing, proof debt, and public-safe output around that boundary.
