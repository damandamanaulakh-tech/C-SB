# What comes out

The output boundary can package **answers**, **plans/ideas**, **code**, **charts/tables**, **tool actions**, and **audio/image references**. The current recovered HTTP API returns structured JSON/text; richer output adapters can be added without changing the numbered brain contracts.
